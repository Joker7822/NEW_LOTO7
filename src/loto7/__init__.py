"""NEW_LOTO7 shared package.

The package is the canonical home for reusable evaluation, validation and
output-layout code. Legacy root and ``scripts`` entry points remain as thin
compatibility wrappers while workflows and resume files migrate gradually.
"""

__version__ = "0.1.0"
